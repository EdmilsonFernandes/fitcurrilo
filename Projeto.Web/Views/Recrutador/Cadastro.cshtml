@model Projeto.Web.Models.RecrutadorViewModel

@{
    Layout = "~/Views/Shared/Layout.cshtml";
}

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">

        <h2>Novo Recrutador</h2>

        @if (TempData["Sucesso"] != null)
        {
            <div class="box-body">
                <h4><span class="label alert-success">@TempData["Sucesso"]</span></h4>
            </div>
        }
        @if (TempData["Falha"] != null)
        {
            <div class="box-body">
                <h4><span class="label alert-danger">@TempData["Falha"]</span></h4>
            </div>
        }


        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Nome, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-4">
                @Html.EditorFor(model => model.Nome, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Nome, "", new { @class = "text-danger" })
            </div>
        </div>

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Email, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-4">
                @Html.EditorFor(model => model.Email, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Cadastrar Recrutador"
                       class="btn btn-default" />
            </div>
        </div>

        <div class="col-md-12">
            <p class=ion-arrow-return-left> <a href="/Recrutador/Consulta">Voltar</a> </p>
        </div>
    </div>
}